# 更新日志

项目的重要变更会记录在本文件中。

## [Unreleased]

## [2.0.3] - 2026-07-17

### 调整

- 移动端终端恢复对齐桌面端与 headless Agent Host 使用的共享原子 baseline 和输出水位协议。
- 切换项目、worktree 和终端时优先复用已有缓存终端会话，不再每次都重复回放完整 baseline。

### 修复

- 修复滚动、调整尺寸、折叠屏开合，以及快速切换项目、worktree 或运行中终端后出现历史重复、顶部残片、空白区域和屏幕恢复不一致的问题。
- 修复终端缓存切换，已有会话不再无条件重复回放完整 baseline；无缓存或存在输出序列缺口的会话仍会请求权威 baseline。
- 修复重连桌面端或 Agent Host 后，切回已有运行中终端时的 viewport owner 恢复问题。
- 修复移动端与桌面端 2.0.3 终端同步和 hosted worktree 路由改动的兼容性。

## [2.0.0] - 2026-07-13

### 新增

- 基于共享 Codux runtime 协议，正式支持从移动端控制远程项目的文件、Git、worktree、终端、AI 活动、记忆、主机监控和设备管理。

### 调整

- 移动端项目、worktree、终端、分屏和标签选择统一到桌面端与 headless Agent 的 runtime model，同时保持移动端 viewport ownership 独立。
- 升级共享 Iroh 传输生命周期，缩短启动、回到前台、切换主机和断线重连时间。

### 修复

- 修复切换项目/worktree、应用进入后台、传输失败、鉴权变化、删除设备和桌面主机重启后的终端空白或旧状态残留。
- 修复长时间运行的 TUI 会话在终端创建、选择、输入、光标位置、scrollback、viewport 接管和恢复方面的问题。

## [2.0.0-rc.9] - 2026-07-11

### 调整

- Iroh 升级至 1.0.2，并将原生控制器 transport 迁移到与共享 endpoint 生命周期一致的进程级运行时。
- 移动端资源订阅与清理对齐桌面端和 headless Agent 的稳定同步模型。本版本不需要重新配对或升级配对协议。

### 修复

- 显著缩短打开 App、从后台恢复和切换主机后的连接时间。
- 修复 App 回到前台时重新启动仍在进行中的连接。
- 移除可能在原生 Iroh 连接完成前提前判定失败的旧超时链路。

## [2.0.0-rc.8] - 2026-07-10

### 调整

- 移动端对齐桌面端与 headless Agent 使用的共享远程终端生命周期和 viewport ownership 模型。

### 修复

- 修复发送失败、Host 错误、终端立即退出、鉴权变化、删除设备或传输断开后创建终端一直卡住的问题。
- 修复远程主机重启和重连后的终端与 worktree 选择，并在创建终端后自动选中新终端。
- 优化切换项目、worktree、终端和主控设备时的 viewport 接管与会话恢复一致性。

## [2.0.0-rc.6] - 2026-07-08

### 调整

- 移动端发布包同步 Codux 2.0.0-rc.6。本 RC 不包含移动端配对协议变更。

### 修复

- 优化移动端与桌面端、agent 在主机重连和重启后的远程终端状态汇总兼容性。

## [2.0.0-rc.5] - 2026-07-07

### 调整

- 移动端发布包同步 Codux 2.0.0-rc.5。本 RC 不包含移动端配对协议变更。

### 修复

- 优化移动端与桌面 Host 最新 baseline keyframe 和终端重绘稳定性改动的远程终端恢复兼容性。

## [2.0.0-rc.4] - 2026-07-06

### 新增

- 移动端终端新增内置 Nerd Font 符号、矢量 powerline 分隔符、更完整的 legacy/braille 字形覆盖、kitty 键盘消歧，以及扩展下划线样式支持。

### 调整

- 移动端发布包同步 Codux 2.0.0-rc.4。本 RC 不包含移动端配对协议变更。

## [2.0.0-rc.3] - 2026-07-05

### 调整

- 移动端发布包同步 Codux 2.0.0-rc.3。本 RC 不包含移动端协议变更。

## [2.0.0-rc.2] - 2026-07-04

### 调整

- 移动端发布包同步 Codux 2.0.0-rc.2。本 RC 不包含移动端协议变更。

## [2.0.0-rc.1] - 2026-07-04

### 调整

- 移动端发布包同步 Codux 2.0.0-rc.1。本 RC 不包含移动端协议变更。

## [2.0.0-beta.8] - 2026-07-02

### 修复

- 修复桌面、手机、平板多端查看 / 接管终端时的恢复问题，活跃会话在 owner 变化后不再出现空白或画面错乱。
- 修复主机 baseline 失败后移动端终端加载卡住的问题，改为依赖明确的 baseline 失败与 stale-output 恢复信号。

### 调整

- 移动端新增 v3.2 终端恢复 capability 解析，包括 baseline 失败、stale output 与 viewport keyframe。
- 降低移动端终端 output ack 频率，同时保持 viewport lease 与 stale-output 恢复的响应性。

## [2.0.0-beta.5] - 2026-07-01

### Changed

- 移动端发布包同步 Codux 2.0.0-beta.5 桌面端与 agent 发布。本 beta 不包含移动端协议变更。

## [2.0.0-beta.4] - 2026-06-30

### Changed

- 移动端发布包同步 Codux 2.0.0-beta.4 桌面端与 agent 发布。本 beta 不包含移动端协议变更。

## [2.0.0-beta.3] - 2026-06-25

### Added

- 通过 iroh mDNS 在同一网络内点对点（p2p）直连 Codux 主机,连接徽章上标明「直连 / 中继」。
- 持久化远程终端:重新回到项目时挂回同一个主机 shell;手机可与桌面共享同一个 agent 终端并交接 viewport(桌面点手机徽章即可收回)。
- 当摄像头实时扫码不顺时,可从相册照片 / 截图导入配对二维码作为兜底。

### Changed

- 扫码配对更清晰:更高摄像头分辨率 + 识别时的加载态;更精简的配对二维码(node id + relay url)并带设备角色徽章。
- 复用暖连接让重连更快,并且不再过早掐断较慢的连接。

### Fixed

- 终端:修复 viewport 尺寸与字形渲染、连接后首行提示符重复 / 残影,以及切换 / 争用时的健壮性;自动恢复卡住的 baseline 以减少延迟导致的卡顿。
- 修复部分 MediaTek/OPLUS 设备的扫码问题(去掉了导致分析饥饿或取景错位的强制摄像头分辨率与自动变焦)。

## [2.0.0-beta.2] - 2026-06-22

### Added

- 在手机上管理 SSH 配置 —— 直接新增、编辑、删除已保存的连接。
- 文件、Git、评审列表均支持下拉刷新。
- 评审底栏一键「提交并推送」「提交并合并」。
- 卡片式文件列表,Git 与评审列表共用统一的列表项样式。

### Changed

- 重构手机/平板工作区:实时与历史 AI 会话展示、独立的「文件」中央视图（编辑器 + 浏览侧栏）、可切换的右侧栏，以及重新设计的 Git 面板。
- 终端输入以硬件键盘优先；右侧栏打开时终端工具栏保持可用。
- 「+」改为新建标签页，不再分屏（分屏仍为桌面端专属）。

### Fixed

- 修复终端方向键与点按输入行为。
- 修复当前目录「/」根路径、文件持久化、同步加载圈时长与评审状态过期问题。

## [2.0.0-beta.1] - 2026-06-20

### Added

- 可连接远程 Codux 主机（`codux-agent`），通过端到端加密的 Iroh 传输，从手机驱动它的终端、Git 与 AI 会话。（Beta）

### Fixed

- 修复自绘终端的光标对齐：光标现在像桌面端一样居中在字形上，并能完整覆盖双宽（中日韩）字符，而不再只覆盖一半。

## [1.9.1] - 2026-06-18

### 调整

- 更新 README 文案和 release 下载链接，匹配当前移动端发布流程。

### 修复

- 修复移动端自绘终端字符测量，终端文本不再出现字符丢失或重叠。
- 修复桌面端到移动端的终端 viewport handoff，controller 驱动的尺寸保持一致。

## [1.9.0] - 2026-06-18

### 调整

- 移动端终端从 Termux / SwiftTerm 原生 view 切换为自绘终端，直接渲染远程终端模型中的共享 cell grid。
- 移除旧 native-rendering 路径才需要的移动端 native-terminal bridge、replay controller、平台插件和孤立 protocol FFI facade。

### 修复

- 修复移动端大历史会话恢复和滚动行为，改为使用共享 raw-history / screen-keyframe 模型，不再依赖原生 replay snapshot。
- 修复远程 viewport ownership，移动端可以驱动终端列数，同时不会把过期 host rows 强塞进 alt-screen TUI 会话。
- 修复终端输入可能通过 IME commit 文本误发送 escape notation 的问题，输入通道诊断 trace 改为显式开启。

## [1.8.3] - 2026-06-17

### 修复

- 修复大历史会话恢复时原生终端回放混入 screen snapshot 后可能清屏、跳光标或出现空白区域的问题，现在只回放 raw terminal history。
- 优化实时终端输出性能，working 输出走 append，baseline、restore 和 screen-only refresh 才走 replace。
- 将移动端恢复历史对齐到 500 行 scrollback 目标，同时保留大终端 buffer 的有界传输保护。

## [1.8.2] - 2026-06-17

### 修复

- 修复移动端工具栏 `Ctrl+C`，现在会直接发送一次原始 ETX，不再重试过期的中断输入。
- 修复 Android / iOS 原生终端回放时，本地 emulator 自动生成的 OSC / DSR 响应被回写到远程 Host 输入流的问题。
- 优化软键盘上推逻辑，改为使用原生终端光标位置，让光标可见且不强制 resize 终端 viewport。
- 优化 Android 终端文字拖选，复用 Termux material 手柄资源，并让手柄锚点与终端选中单元格对齐。
- 补充 iOS bundle 本地化元数据，原生终端选择控件可以跟随应用 / 设备语言。

## [1.8.1] - 2026-06-16

### 调整

- 移动端远程传输说明和发布元数据对齐 Iroh-only 桌面端 / 主控端协议。
- 将移动端发布版本更新到 1.8.1，用于发布共享桌面 / 移动端 Iroh 传输和终端稳定性修复。

### 修复

- 修复旧 relay / WebRTC 链路从共享协议移除后，移动端配对和重连说明不一致的问题。
- 移动端发布记录同步 1.8.1 源码 tag 中的共享 runtime、protocol FFI、记忆提取和终端稳定性修复。

## [1.8.0] - 2026-06-14

### 新增

- 新增移动端主控链路对共享 Rust protocol FFI 的使用，移动端与桌面端消费同一套协议 payload、transport 元数据、延迟事件和终端布局消息。
- 新增移动端通过 Host runtime model 创建分屏、标签、worktree 和终端布局操作的能力。
- 新增移动端显示设置，应用文字和终端文字分开调整，终端字号提供从小到特大的档位。

### 调整

- 移动端终端屏幕改为从共享 headless terminal model 渲染，不再走独立 Dart 终端历史链路。
- 重构项目、worktree、分屏和标签选择，移动端保留自己的 active UI selection，同时接收共享 Host 的项目 / worktree / 终端关系模型。
- 优化 iOS 和 Android 上的终端滚动、光标绘制、字符宽度测量、输入法转发和 TUI 恢复行为。
- 清理移动端中已由共享 runtime、protocol 和 terminal crates 覆盖的重复兜底逻辑。

### 修复

- 修复切换项目、切换 worktree、后台恢复或桌面 Host 重启后移动端终端偶发空白的问题。
- 修复移动端创建分屏 / 标签不同步到桌面端，以及桌面端创建布局变化不稳定同步到移动端的问题。
- 修复删除分屏 / 标签后移动端残留旧布局状态的问题，并处理删除最后一个终端的无效场景。
- 修复正常实时输出和项目切换时历史加载遮罩反复闪烁的问题。
- 修复大 TUI 会话恢复后只显示一屏、scrollback 丢失或光标 / 输入位置错误的问题。
- 修复输入法退格 / 删除、终端点击聚焦、终端字体回退和移动端光标绘制回归。
- 修复直连 / 中继切换或桌面端重启后延迟和传输路径标签不更新的问题。

## [1.7.5] - 2026-06-09

### 新增

- 新增 v3.1 host capability 解析、终端 buffer 分片组包、按会话维护终端 replica、终端订阅 scope 和大历史有界渲染。
- 新增终端输入可靠发送、输出排序 / 重同步、上传元数据和协议 payload codec，为后续跨设备 runtime domain 做准备。
- 新增移动端更新检查，优化调试日志导出，并补充 Codux Mobile 作为主控端的文档说明。

### 调整

- 将移动端远程工作区拆分为 runtime store、同步、项目、终端、设备、文件、日志、设置和 widget 等独立层，UI 只负责渲染状态和触发用户意图。
- 重构终端页面，当前项目 / session 选择只负责挂载本地 replica，不再决定协议消息是否接收。
- 将项目、文件、worktree、设备操作、调试日志和更新 UI 拆成独立 widget，并补齐对应 service/controller 测试。

### 修复

- 修复首次进入、切换项目、桌面 Host 重启和快速切换分屏时偶发终端空白的问题。
- 修复桌面 Host 重启后移动端可能继续显示旧终端缓存的问题，将 Host 确认的 runtime 状态与 UI 挂载的终端 pane 分离。
- 修复弱网下终端历史恢复不稳定的问题，增加重复分片保护、乱序组包、输入重试和输出重同步辅助逻辑。
- 修复同步回包和列表刷新噪音，重复的 project / terminal list 不再反复触发冲突的 UI 状态变化。

## [1.7.1] - 2026-06-08

### 调整

- 将移动端远程状态收敛到设备选择、连接同步、runtime store 和同步状态服务中，UI 只触发意图，项目与终端决策由 runtime store 统一处理。
- 将终端全量 buffer 恢复限制为有界窗口，Codex 恢复大历史时在较慢移动网络下也能保持可用。
- 新增 v3.1 host capability 解析和终端 buffer 分片组包，让移动端按桌面端同一套协议 API 消费远程数据。

### 修复

- 修复加密消息 replay 判断过于依赖全局序号的问题，跨通道小范围乱序不再误丢 `project.list`、`terminal.list`、`host.info` 或终端输出消息。
- 修复快速切换项目时 `project.selected`、刷新后的项目 / 终端列表、会话绑定、终端 resize 和终端 buffer 恢复无法稳定闭环导致的空终端问题。
- 修复刷新后的项目列表早于对应终端列表到达时，可能重复发送缺失终端恢复请求的问题。
- 修复首次进入和重新打开时的设备恢复链路，记录最近可响应设备，并让缓存项目与 Host 确认的选中项目状态保持一致。
- 修复超大终端历史加载，通过分片组包提供进度、重复分片保护、乱序到达支持和移动端内存上限保护。

## [1.7.0] - 2026-06-08

### 新增

- 正式接入 Codux Mobile v3 远程协议，与 Codux Desktop 共用 Relay / WebRTC 模型。
- 新增无状态二维码 ticket 配对，桌面端通过中继服务发布短期配对载荷。
- 新增 WebRTC DataChannel 直连传输和 WebSocket 中继回退，并通过协议 ping/pong 显示延迟。
- 新增应用内调试日志，支持复制、导出和日志等级配置，方便排查连接与终端显示问题。

### 调整

- 统一项目列表、终端列表、host info、传输状态和重连逻辑，收敛到同一套远程协议状态机。
- 重构首次同步，连接、hello 和传输路径事件都会主动拉取 host info、项目列表、终端列表和终端 buffer。
- 原生终端 controller 创建或 resize 后会回放当前 buffer，避免恢复会话时打开空白终端。

### 修复

- 修复首次配对或重启应用后进入设备，终端为空，必须手动切换项目才显示的问题。
- 修复前后台切换时连接状态跳动、延迟不显示、Relay / P2P 状态标签滞后的问题。
- 修复项目切换和分屏选择时重复请求终端 buffer 与回放顺序异常的问题。

### 说明

- Codux Mobile 1.7.0 建议搭配 Codux Desktop 1.7.0 使用，升级后建议重新配对已有设备。

## [1.7.0-beta.1] - 2026-06-07

### 调整

- 将移动端远程传输切换到 v3 Relay / WebRTC 模型。
- 直连可用时优先使用 WebRTC DataChannel，P2P 不可用时回落 WebSocket 中继。
- 保存配对 payload 中的传输候选，让重连流程和桌面端使用同一套协议模型。

### 说明

- 此测试版需要配合新版 Codux 桌面端和新版 Codux 中继服务使用，已有远程设备需要重新配对。

## [1.6.8] - 2026-06-07

### 调整

- 发布更高 Android 构建号用于正式渠道 P2P 验证，不改动传输业务逻辑。

## [1.6.7] - 2026-06-07

### 修复

- 将 Android `INTERNET` 权限加入 main release Manifest，确保正式打包版本和 debug 版本一样可以使用 Iroh 网络传输。

## [1.6.6] - 2026-06-07

### 修复

- 普通重连时忽略本地保存的 Iroh direct 地址，只使用稳定的 node 身份和 relay 拨号。
- 仅在当前 `host.info` 返回新 direct 地址时，执行受控的中继到直连升级重连。
- 当当前会话已经到达直连链路后，避免后续中继回落触发重复升级重连。

## [1.6.4] - 2026-06-06

### 修复

- 修复 Iroh direct 地址更新没有真正进入原生地址簿的问题，host 上报的新地址会写入 native lookup。
- 避免进入终端、返回主机列表、切换项目和刷新 host info 时误触发重连，只有原生链路明确为中继或 mixed 时才尝试升级。
- 为中继到直连的升级重连增加冷却，避免 direct 不可用时形成重连循环。

## [1.6.3] - 2026-06-06

### 修复

- 修复升级到 Iroh 1.0.0-rc.1 后 Android 端 Endpoint 启动卡在连接中的问题，补齐 Iroh DNS Resolver 所需的 Android 原生 Context 初始化。
- 增加原生 Iroh Endpoint bind 阶段状态和超时错误，后续连接启动异常不会再只停留在“连接中”。

## [1.6.2] - 2026-06-05

### 调整

- 将移动端 Iroh 传输桥接升级到 `iroh` 1.0.0-rc.1，并把配置的 relay URL 传入原生传输层。
- 打开项目时优先进入第一个 Split 终端，使默认终端和桌面端布局保持一致。

### 修复

- 修复 iOS bridge 导出，确保 Swift 插件可以调用 `codux_iroh_add_node_addr`。
- 为 iOS Iroh bridge 链接 `Network.framework`，并用当前构建身份重签 Flutter native asset 嵌入框架。

## [1.6.0] - 2026-06-05

### 新增

- 新增移动端原生 Iroh 远程传输桥接，支持配对、重连、终端流量和上传传输。
- 新增终端切换页面，支持 Split 终端、Tab 终端和 Worktree 切换。
- 新增移动端 Worktree 新建、合并、删除和刷新操作。

### 调整

- 用统一的 Iroh QUIC 协议链路替换旧的 relay/WebRTC 终端传输。
- 标准化配对和重连流程，业务协议和加密信封继续由 Dart 处理，原生传输层只负责连接和帧传递。
- 文件和图片上传仅允许在 Iroh 直连路径下执行，避免大文件走 relay。
- 更新 iOS TestFlight 工作流，在归档前构建 Iroh bridge。

### 修复

- 通过 Iroh n0 discovery 修复桌面端重启后移动端无法重新解析 node 地址的问题。
- 修复新传输链路下项目切换、终端历史恢复、连接状态、延迟显示和 Host 响应探测问题。
- 修复 iOS 页面切换动画、右滑返回、终端边距、工具栏布局、配对文案溢出和上传文案问题。

## [1.5.0] - 2026-06-04

### 调整

- 移动端对齐 Codux 1.5.0 桌面端协议和 GPUI 终端 Host。
- 优化 Android 和 iOS 的共享终端恢复、尺寸同步、分屏顺序和移动端终端渲染。
- 在 `host.info` 握手中增加远程协议版本检查，桌面端不兼容时提示用户升级应用，不再静默连接。

### 修复

- 移除 iOS 嵌入终端里的 Ghostty 键盘工具条。
- 修复新桌面运行时下 Android 终端背景、字号尺寸、分屏列表残留和项目切换问题。

### 说明

- 包含 1.5.0-beta.1 移动端测试周期中的验证内容，覆盖 Codux 1.5.0 桌面协议、共享终端恢复、分屏顺序、Android 和 iOS 终端渲染，以及 Host 协议兼容检查。

## [1.5.0-beta.1] - 2026-06-03

### 调整

- 移动端对齐 Codux 1.5.0 桌面端协议和 GPUI 终端 Host。
- 优化 Android 和 iOS 的共享终端恢复、尺寸同步、分屏顺序和移动端终端渲染。
- 在 `host.info` 握手中增加远程协议版本检查，桌面端不兼容时提示用户升级应用，不再静默连接。

### 修复

- 移除 iOS 嵌入终端里的 Ghostty 键盘工具条。
- 修复新桌面运行时下 Android 终端背景、字号尺寸和分屏列表残留问题。

## [0.1.11] - 2026-05-24

### 调整

- 调整 Android 自适应图标前景比例，使其更接近 iOS 和 macOS 图标的视觉大小。

### 修复

- 补充 iOS 定位权限用途说明，解决 App Store Connect 对附近连接相关 API 的静态扫描警告。

## [0.1.10] - 2026-05-23

### 新增

- 新增终端工具栏文件上传，图片上传继续使用相同的路径插入流程。
- 新增 P2P 健康探测，只有 DataChannel 已打开且持续响应时才显示为 P2P。

### 调整

- 上传只走独立的 WebRTC upload DataChannel，避免大文件回退到中继或堵塞终端流量。
- 文件和图片上传必须存在健康的 P2P 直连上传通道，否则直接阻止上传。
- 上传完成后插入终端的路径增加平台转义，带空格路径可正常使用。

### 修复

- 优化项目切换后的终端历史恢复，优先走直连 P2P buffer 请求并避免 relay 重复数据。
- 修复文件上传状态文案，并增加上传传输被拒绝时的测试覆盖。

## [0.1.9] - 2026-05-22

### 修复

- 固定 `device_info_plus` 和 `package_info_plus` 到兼容组合，确保 iOS 归档可在当前 GitHub macOS runner SDK 上构建。

## [0.1.8] - 2026-05-22

### 修复

- 固定 `device_info_plus` 到兼容当前 iOS SDK 的版本，确保 GitHub Actions 可以正常归档 iOS TestFlight 构建。

## [0.1.7] - 2026-05-22

### 新增

- 新增 iOS TestFlight 发布自动化，支持上传到 App Store Connect。
- 新增基于 Ghostty 的 iOS 原生终端适配器，iOS 与 Android 共用 Dart 侧终端会话流程。
- 新增设备列表和终端标题栏延迟显示。

### 调整

- 完善 iOS 发布签名、Bundle 元数据、应用图标、启动图和 App Store 更新检查，支持 TestFlight 分发。
- 优化扫码配对和粘贴相关文案，避免紧凑移动端布局下溢出。

## [0.1.6] - 2026-05-16

### 调整

- Android 正式发布和手动 Release 构建改为只输出 `arm64-v8a` APK 包。
- 优化终端传输稳定性，新增 Host 响应探测，避免只有 relay 在线时误显示为 Mac 已连接。
- 优化 WebRTC 终端传输的背压、重连和 relay 兜底逻辑，低流量状态下保持更稳定。
- 优化图片上传体验，改为终端切片上传，上传和插入期间持续显示进度/加载状态。

### 修复

- 修复 Mac 软件关闭后移动端仍在“同步中 / 中继”之间循环的问题，现在会进入连接失败并后台重试。
- 修复 App 从后台回到前台后终端内容和 Host 快照刷新不及时的问题。
- 修复终端输入和输出的重复处理问题，增加输入确认和输出序号确认。

## [0.1.5] - 2026-05-05

### 新增

- 新增共享 Mac 终端分屏能力，支持移动端切换、新建、删除分屏、历史回放，以及由移动端尺寸驱动 resize。
- 新增基于 WebRTC DataChannel 的远程终端 P2P 传输，优先 STUN 直连，失败后回退 WebSocket 中继。
- 新增本地 sherpa_onnx 语音输入，使用应用内波形浮窗和可编辑识别结果预览。
- 新增终端输入 payload 规范化，粘贴、语音、键盘输入和控制键统一走稳定插入链路。

### 调整

- 移除 Android 原生语音识别入口，改为本地语音模型链路。
- P2P STUN 顺序会根据语言环境选择，中文环境优先国内 STUN，同时保留海外 STUN 兜底。
- 优化连接状态宽限处理，短暂重连时设备列表保持可观测，不再频繁闪断。

### 修复

- 修复 IME 组合输入、粘贴和语音发送路径导致的终端重复输入问题。
- 修复共享 Mac 终端会话下按回车出现额外空行的问题。
- 修复 Mac 重启或移动端切换项目后的终端历史加载、后台项目和会话同步问题。
- 修复 Android 原生终端视图重建后的事件重复订阅和重复 resize 问题。

## [0.1.4] - 2026-04-28

### 调整

- 项目列表同步反馈改到右侧项目操作按钮的 loading 图标中，横向项目列表不再显示临时状态文字。
- 项目列表空状态只在 Host 同步完成且确实没有项目时显示“暂无项目”。

## [0.1.3] - 2026-04-28

### 修复

- 移动端重连后保持加密消息序号单调递增，避免 Mac Host 将新的 `project.list` 和 `terminal.list` 请求误判为重放消息并丢弃。
- UI 区分 relay 连接和 Host 快照同步状态，项目/终端快照未返回时显示同步中，不再误报完全已连接。
- App 回到前台时重新建立连接，避免 Android 暂停/恢复后沿用失效 WebSocket 状态。
- 忽略 Android 重建终端原生视图后的旧 resize 调用，避免终端视图竞态触发 `MissingPluginException`。

## [0.1.2] - 2026-04-28

### 修复

- 为初始 `project.list` 和 `terminal.list` 快照请求增加未返回重试，移动端项目列表和终端会话查询可从偶发消息丢失中恢复。
- App 启动时先恢复本地缓存项目列表，并在 Host 返回最新列表后刷新缓存。
- 终端历史 loading 只在当前会话真实发起 `terminal.buffer` 请求后显示，避免项目或会话还没回来时卡在加载状态。
- 新增项目列表重试、项目列表缓存、项目列表返回前进入终端的回归测试。

## [0.1.1] - 2026-04-28

### 新增

- 新增终端历史加载状态，避免进入终端页时只看到空白光标、看不出正在恢复远端历史。

### 修复

- 为 `terminal.buffer` 历史补拉增加未确认重试，改善中继重连或消息偶发丢失后终端历史不显示的问题。
- 新增终端历史补拉的重试、确认和 ready 状态回归测试。

## [0.1.0] - 2026-04-28

### 新增

- 新增第一版 Codux Mobile Flutter 客户端，通过 relay 服务连接 macOS 端 Codux。
- 支持扫码配对、设备管理、项目切换、终端分屏切换、文件浏览、图片上传和 AI 用量面板。
- 新增基于 Termux TerminalView 的 Android 原生终端 Flutter 插件，支持远程输出、用户输入、滚动历史、文字选择、快捷键和输入法避让。
- 新增 GitHub 更新检查，读取 `duxweb/codux-flutter` 最新 Release。

### 调整

- 移除早期 WebView / xterm 方向，移动端终端改为原生 Android 插件渲染。
- 新增 `CODUX_LOG_LEVEL` 发布日志控制，Flutter 层和原生终端插件共用同一日志等级。

### 修复

- 稳定 Android 输入法避让策略，TUI 程序输入时移动终端显示区域，不再强制触发远端终端 resize。
- 分离用户输入与本地终端响应通道，修复远程终端输入重复和本地 emulator response 回灌问题。
